test:
  build:
    image: sublime3
    dockerfile: Dockerfile
  environment:
    PACKAGE: 'MarkdownLink'
    SUBLIME_TEXT_VERSION: '3'

node:
  image: node:12
  volumes:
    - '.:/wd'
  working_dir: /wd
  encrypted_env_file:
    - env.encrypted
